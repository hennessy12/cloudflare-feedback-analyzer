{
  "name": "feedback-analyzer",
  "version": "1.0.0",
  "description": "Cloudflare-powered feedback aggregation and analysis tool",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:init": "wrangler d1 execute feedback-db --local --file=./schema.sql",
    "d1:init-remote": "wrangler d1 execute feedback-db --remote --file=./schema.sql"
  },
  "keywords": ["cloudflare", "workers", "ai", "feedback"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "wrangler": "^3.94.0"
  }
}
